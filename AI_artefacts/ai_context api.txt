"Input POST api/v1/photo/auth  Параметры вытягиваются из тела запроса: {   ""vk_user_id"": integer,   ""vk_app_id"": integer,   ""vk_is_app_user"": boolean,   ""vk_are_notifications_enabled"": boolean,   ""vk_language"": string,   ""vk_access_token_settings"": string,   ""vk_platform"": string,   ""sign"": string }  Логика api/v1/photo/auth  1) Валидируем параметры запроса  Def mech 1 == 1  Если параметры указаны некорректно (Def mech 1 == 0) - возвращаем 400 и текст ошибки:  Output api/v1/photo/auth: http 400: {     ""error"": ""Missing required parameter""}  2) Верифицируем vk_id по алгоритму вычисления подписи из параметров ['vk_':] и сравнения с параметром sign: (Def Mech 2) == 1 - подписи совпали  Если подписи не совпали (Def Mech 2) == 0 - произошла подмена, значит, возвращаем 403 и текст сообщения:  Output api/v1/photo/auth: http 403: {    ""message: ""Access Denied""  }  3) генерируем токен: (Def Mech 3) == auth_token  Output api/v1/photo/auth: http 200: {     ""auth_token"": ""<токен>"" }   "
" INPUT  GET /v1/photos/list  Опциональный параметр - full_fields (boolean) (по умолчанию - true)  Параметры запроса в заголовке, помимо технических:  {   auth_token = <токен> string // Опциональный параметр (тип: string, значение: токен авторизации, выданный через /auth)   }  Логика: Валидация auth_token (если передан): проверка существования в БД tokens и срока действия, получение data_token  - если токен некорректный:  403 {   ""error"": ""Invalid authentication token."" }  Подготовка данных (Def Mech 5) = data  Возвращаем data  200 [   {     id_string,     status,     ....,     load_time,     dublicate_group_id_string,     identity_photo_group_id_string,   },   ...  ]"
"Input POST /v1/photos/upload  Формат: multipart/form-data 1. files[]: Массив файлов (до 10 шт) (тип: jpeg/png, вес: до 3 МБ каждое) 2. auth_token: string Опциональный параметр (тип: string, значение: токен авторизации, выданный через /auth)    Логика:  Валидация запроса   1. Проверка максимального веса файлов (≤ 3 МБ). 2. Проверка типов (только JPG, PNG).  3. Проверка длины массива (≤ 10 файлов).    - Если какой то критерий не соблюден: 400 {   ""error"": ""<текст ошибки>"",   ""<формулировка критерия>"":<ограничение>  }  Валидация auth_token (если передан): проверка существования в БД tokens и срока действия, получение data_token  - если токен некорректный:  403 {   ""error"": ""Invalid authentication token."" }  Подготовка данных   1. Для каждого файла генерируется уникальный id_string (UUID). 2. Рассчитывается object_key для MinIO: photos/<id_string>XOR<data_token>.<ext>. 3. Извлекается вес фото в МБ из параметров файла   Сохранение в MinIO   1. Загрузка оригинальных файлов в bucket photos.   2. Фиксация времени (load_time).  Вставка данных в БД photos для каждого фото вставляем все ранее подготовленные данные в соответствующие поля. если токен действителен - is_from_miniapp = true и owner_auth_token = data_token  Отправка задач в кафку {   ""photo_id"": ""<photo_id (int)>"",   ""object_key"": ""<object_key>"" }   - если на каком то из этапов инфра падает:  500 {   ""error"": ""<текст ошибки (какой сервис упал)>"", }  Изменение статуса каждой фотки в бд Photos на pending  202  {   ""status"": ""pending"",   ""photo_ids"": [""id_string_1"", ""id_string_2""],   ""message"": ""Files accepted for asynchronous processing."" }      "
" INPUT  GET /v1/photos/{photo_id} Обязательный параметр - photo_id (string)  Опциональный параметр - full_fields (boolean) (по умолчанию - true)  Параметры запроса в заголовке, помимо технических:  {   auth_token = <токен> string // Опциональный параметр (тип: string, значение: токен авторизации, выданный через /auth)   }  Логика:  Валидация {duplicate_group_id_string} (проверка формата UUID) 1. если формат невалидный:  400 { ""error"": ""Invalid photoID format."" }  Проверка наличия фото в БД photos где id_string = {photo_id}  1. если фото не найдена: 404 { ""error"": ""Photo not found."" }   Валидация auth_token (если передан): проверка существования в БД tokens и срока действия, получение data_token. Проверка владения фото пользователем   - если токен некорректный или фото не принадлежит пользователю:  403 {   ""error"": ""Invalid authentication token."" }  Подготовка данных (Def Mech 6) = data  Возвращаем data  200    {     id_string,     status,     ....,     load_time,    dublicate_group_id_string,    identity_photo_group_id_string,    // и если status == done:    faces_count,    eyes_closed_count,    is_blured,    blur_score,    quality_metrcis,    light_level,   } "
"INPUT GET /v1/duplicate-groups/{duplicate_group_id_string}  {   auth_token = <токен> string // Опциональный параметр (тип: string, значение: токен авторизации, выданный через /auth). Передается в заголовке Authorization}  Логика:  Валидация {duplicate_group_id_string} (проверка формата UUID) 1. если формат невалидный:  400 { ""error"": ""Invalid group ID format."" }  Поиск группы в БД Groups где id_string = {duplicate_group_id_string}  1. если группа не найдена: 404 { ""error"": ""Duplicate group not found."" }  Определение прав доступа:  Если auth_token передан:  1. Если token_invalid:  403 {   ""error"": ""Invalid authentication token."" }  2. Достаем owner_data_token найденной группы. Если owner_token != auth_token: 403 {   ""error"": ""Access denied."" }  Если auth_token НЕ передан:  3. Достаем owner_token найденной группы. Если owner_token != NULL: 403 {   ""error"": ""Authentication required for private groups."" }  Подготовка данных (Def Mech 8) = data Возвращаем data  200    {     photo_id_string   },  "
" INPUT  GET /v1/dublicate-groups/list  {   auth_token = <токен> string // Опциональный параметр (тип: string, значение: токен авторизации, выданный через /auth)   }  Логика: Валидация auth_token (если передан): проверка существования в БД tokens и срока действия, получение data_token  - если токен некорректный:  403 {   ""error"": ""Invalid authentication token."" }  Подготовка данных (Def Mech 7) = data  Возвращаем data  200 [   {     id_string,     is_identity_group     created_at,     photos_count    },   ] "
" INPUT  GET /v1/dublicate-groups/list  {   auth_token = <токен> string // Опциональный параметр (тип: string, значение: токен авторизации, выданный через /auth)   }  Логика: Валидация auth_token (если передан): проверка существования в БД tokens и срока действия, получение data_token  - если токен некорректный:  403 {   ""error"": ""Invalid authentication token."" }  Подготовка данных (Def Mech 7) = data  Возвращаем data  200 [   {     id_string,     is_identity_group     created_at,     photos_count    },   ] "